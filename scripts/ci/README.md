# CI Scripts

Shared shell helpers for GitHub Actions and GitLab CI live here.

- `lib.sh` contains the reusable functions.
- `github/` wrappers stay focused on GitHub output plumbing.
- `gitlab/` wrappers stay focused on GitLab job behavior.
